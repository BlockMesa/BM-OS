print("Login on device "..kernel.hostname())
term.write("Username:")
local user = read()
term.write("Password:")
local password = read("*")
kernel.login(user, password)
os.run({},"/bin/sh.lua")
